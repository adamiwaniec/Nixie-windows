[package]
name = "nixie"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
cuda-system = [
    "nixie_common/cuda-system",
    "cudarc/cuda-version-from-build-system",
    "cudarc/dynamic-linking",
]
cuda-12 = [
    "nixie_common/cuda-12",
    "cudarc/cuda-12000",
    "cudarc/fallback-dynamic-loading",
]
cuda-13 = [
    "nixie_common/cuda-13",
    "cudarc/cuda-13000",
    "cudarc/fallback-dynamic-loading",
]
profiling = []
default = ["cuda-system"]

[dependencies]
nixie_common = { path = "../common", default-features = false }
bincode.workspace = true
tarpc.workspace = true
thiserror.workspace = true
futures.workspace = true
serde.workspace = true
colored.workspace = true
cudarc.workspace = true

async-bincode = "0.7.2"
clap = { version = "4.5.4", features = ["derive"] }
tokio = { version = "1.38.0", features = [
    "rt-multi-thread",
    "net",
    "sync",
    "macros",
    "signal",
    "io-util",
    "time",
    "fs",
] }
tokio-serde = { version = "0.9.0", features = ["serde_cbor", "cbor"] }

tracing = "0.1.40"
tracing-subscriber = { version = "0.3.20", features = [
    "env-filter",
    "std",
    "fmt",
    "json",
    "registry",
] }

is-root = "0.1.3"
chrono = "0.4.38"
hashlink = "0.10.0"
nvml-wrapper = "0.10.0"
toml = "0.8.20"
bytes = "1.10.1"
itertools = "0.14.0"
serde_json = "1.0.145"
tempfile = "3.23.0"
mimalloc = { version = "0.1.48", features = ["v3"] }
clap_complete = "4.6.0"

[target.'cfg(unix)'.dependencies]
nix.workspace = true
syscalls = { version = "0.6.18", features = ["x86_64"] }

[target.'cfg(windows)'.dependencies]
windows.workspace = true
